import 'package:flutter/material.dart';

class KameraPage extends StatelessWidget {
  const KameraPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Kamera Page'),
    );
  }
}
